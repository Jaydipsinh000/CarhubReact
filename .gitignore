# Ignore the Android project folder
android/

# IDE settings
.idea/
# (specific Android files were listed here mistakenly; remove them)
# Use `git rm -r --cached android` to untrack existing files instead.
